import fs from 'node:fs';const x=JSON.parse(fs.readFileSync('records/checkpoints.json','utf8')).checkpoints;if(x.length!==10)throw Error('need ten');if(new Set(x.map(i=>i.stage_id)).size!==10)throw Error('duplicate');if(x.filter(i=>i.cold_recall).length<4)throw Error('need cold stages');console.log(`timeline: PASS (${x.length} stages, ${x.filter(i=>i.cold_recall).length} cold recalls)`);
